import { Schema, model, Types } from 'mongoose';

export const DOCUMENT_NAME = '{{moudleName}}';
export const COLLECTION_NAME = '{{moudleName}}s';

export interface {{moudleName}} {
    _id: Types.ObjectId;
    name: string;
    createdAt: Date;
    updatedAt: Date;
}

const schema = new Schema<{{moudleName}}>(
    {
        name: {
            type: Schema.Types.String,
            required: true,
            trim: true,
            unique: true
        }
    },
    { timestamps: true }
);

export const {{moudleName}}Model = model<{{moudleName}}>(DOCUMENT_NAME, schema, COLLECTION_NAME);
