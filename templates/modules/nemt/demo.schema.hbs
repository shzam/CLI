import Joi from 'joi';
import { JoiObjectId } from '@helpers/validator';

export default {
    {{moduleNameLowercase}}Schema: Joi.object().keys({
        name: Joi.string().required()
    }),
    {{moduleNameLowercase}}Id: Joi.object().keys({
        id: JoiObjectId().required()
    })
};
